@model InventoryManager.Models.Inventory
@{
    ViewData["Title"] = "Редактировать инвентарь";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <div class="d-flex align-items-center mb-4">
                <a asp-action="Index" class="btn btn-outline-secondary btn-sm rounded-pill me-3">
                    <i class="bi bi-arrow-left"></i>
                </a>
                <h2 class="fw-bold mb-0">Редактировать инвентарь</h2>
            </div>

            <div class="card border-0 shadow-sm">
                <div class="card-body p-4">
                    <form asp-action="Edit" method="post">
                        <div asp-validation-summary="All" class="text-danger mb-3"></div>

                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="CreatedById" />
                        <input type="hidden" asp-for="CreatedAt" />
                        <input type="hidden" asp-for="Version" />

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label fw-medium">Название</label>
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label fw-medium">Описание</label>
                            <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Category" class="form-label fw-medium">Категория</label>
                            <select asp-for="Category" asp-items="ViewBag.Categories" class="form-select"></select>
                        </div>

                        <div class="mb-4">
                            <div class="form-check form-switch">
                                <input asp-for="IsPublic" class="form-check-input" type="checkbox" role="switch" />
                                <label asp-for="IsPublic" class="form-check-label">Публичный инвентарь</label>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label for="TagsInput" class="form-label fw-medium">Теги</label>
                            <input type="text" name="TagsInput" id="TagsInput" class="form-control"
                                   value="@(string.Join(", ", Model.InventoryTags.Select(it => it.Tag?.Name ?? "")))"
                                   placeholder="электроника, книги, мебель (через запятую)" />
                            <small class="text-muted">Укажите теги через запятую</small>
                        </div>

                        <hr />
                        <h5 class="fw-bold mb-3">Кастомные поля</h5>

                        @{
                            var fieldGroups = new[]
                            {
                                new { Prefix = "CustomString", Label = "Текстовые поля (string)", Icon = "bi-type" },
                                new { Prefix = "CustomText",   Label = "Многострочные поля (text)", Icon = "bi-text-paragraph" },
                                new { Prefix = "CustomInt",    Label = "Числовые поля (number)", Icon = "bi-123" },
                                new { Prefix = "CustomBool",   Label = "Чекбокс поля (boolean)", Icon = "bi-toggle-on" },
                                new { Prefix = "CustomLink",   Label = "Поля-ссылки (URL)", Icon = "bi-link-45deg" },
                            };
                        }

                        @foreach (var group in fieldGroups)
                        {
                            <div class="mb-3 p-3 border rounded">
                                <h6 class="text-muted mb-3">
                                    <i class="bi @group.Icon me-1"></i> @group.Label
                                </h6>
                                @for (int i = 1; i <= 3; i++)
                                {
                                    var stateProp = $"{group.Prefix}{i}State";
                                    var nameProp = $"{group.Prefix}{i}Name";
                                    var descProp = $"{group.Prefix}{i}Description";
                                    var showProp = $"{group.Prefix}{i}ShowInTable";
                                    var isEnabled = (bool?)Model.GetType().GetProperty(stateProp)?.GetValue(Model) == true;
                                    var nameVal = Model.GetType().GetProperty(nameProp)?.GetValue(Model)?.ToString();
                                    var descVal = Model.GetType().GetProperty(descProp)?.GetValue(Model)?.ToString();
                                    var showVal = (bool?)Model.GetType().GetProperty(showProp)?.GetValue(Model) == true;

                                    <div class="border rounded p-3 mb-2 bg-light">
                                        <div class="d-flex align-items-center gap-3 mb-2">
                                            <div class="form-check form-switch mb-0" title="Включить поле">
                                                <input type="checkbox" name="@stateProp" value="true" class="form-check-input custom-field-toggle"
                                                       role="switch" @(isEnabled ? "checked" : "") />
                                                <input type="hidden" name="@stateProp" value="false" />
                                            </div>
                                            <input type="text" name="@nameProp" class="form-control form-control-sm"
                                                   placeholder="Название поля @i" value="@nameVal" />
                                        </div>
                                        <div class="d-flex align-items-center gap-3">
                                            <input type="text" name="@descProp" class="form-control form-control-sm"
                                                   placeholder="Подсказка для пользователя (необязательно)" value="@descVal" />
                                            <div class="form-check form-switch mb-0 text-nowrap" title="Показывать в таблице">
                                                <input type="checkbox" name="@showProp" value="true" class="form-check-input"
                                                       role="switch" @(showVal ? "checked" : "") />
                                                <input type="hidden" name="@showProp" value="false" />
                                                <label class="form-check-label small text-muted">В таблице</label>
                                            </div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }

                        <div class="d-flex gap-2 mt-4">
                            <button type="submit" class="btn btn-primary rounded-pill px-4">
                                <i class="bi bi-check-lg me-1"></i> Сохранить
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">Отмена</a>
                        </div>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}